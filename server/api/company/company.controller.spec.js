'use strict';

var should = require('should');
var app = require('../../app');
var ctrl = require('./company.controller.js');

/*
describe('GET /api/companies', function() {

  it('should respond with JSON array', function(done) {
    done();
  });
});
*/
